server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name ${ROOT_HOST} _;

    location ^~ /.well-known/ {
        root /usr/share/nginx/html;
        try_files $uri =404;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
